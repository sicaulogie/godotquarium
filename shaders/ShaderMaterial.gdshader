shader_type canvas_item;

uniform float alpha : hint_range(0.0, 1.0) = 0.5;

void fragment() {
    vec4 col = texture(TEXTURE, UV);
    // Treat black as transparent
    float brightness = max(col.r, max(col.g, col.b));
    col.a = brightness * alpha;
    COLOR = col;
}